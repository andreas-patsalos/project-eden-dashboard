/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./static/**/*.html", // Scan all HTML files in the static folder
    "./static/**/*.js"    // Scan all JS files in the static folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}